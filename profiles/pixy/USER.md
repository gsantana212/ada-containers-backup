Giovanni "Gio" Santana (G-I-O from "Giovanni"). Mesh owner; voice+short Telegram; sparse/inferring intent. GitHub+Gmail OAuth; OpenCode Go plan (paid). Pre-2026-06-19 was "Alexander" — his AI renamed to **Ada** (was "Big Boss").
§
Real Ada answers all comms (Telegram primary; Element/Matrix = file-relay, NOT Claude/OpenRouter bot). Workers never idle (auto-feeder pending<3). MRR repos PRIVATE.
§
**Real artifacts + parallelism (2026-08-22 + reinforced 2026-08-23):** deliverable is the FILE/verdict/scaffold, NOT the plan/report. Sector reports had PHASE 3 test commands nobody ran; Gio noticed. **Always close the loop**: every Action recommendation → real run + verdict in /root/.hermes/research/test-verdicts.md OR scaffold in /root/.hermes/projects/<name>/ with re-runnable tests/smoke.sh + ada-promote/<name>.json entry. NEVER cite a tool's behavior without running it; applies to research briefs, MRR recommendations, skill recommendations, code reviews. **ALWAYS dispatch parallel sub-agents simultaneously** (evas, parallel terminal jobs, parallel tool calls); never serial when parallel is possible. Wait all → read all → synthesize one combined result. Foreground `&` blocked — use `background=true` + `notify_on_complete=true` for bounded jobs; pair with separate health-check calls rather than blind sleeps.
§
**Status format (LOVES, 2026-06-20):** ✅/❌/🟡 tables > prose, "i love like this" 2026-06-20. One "next action" callout. Voice on top when natural. Short msgs, voice notes, screenshots, abbreviations ("ty"=thanks). **Parallel always** — never stop workers.
§
**Self-educate proactively (2026-06-20):** "look up docs and wikis on every tech stack we use so you can master all you use" = research + KB doc + skill on request.
§
**Full delegation (2026-06-20):** "all you choose", "the server is yours 100%", "i trust you" = auto-execute, no permission asks. Process voice memos inline.
§
**Install rule (2026-08-28):** "you can install if not redundants" — research-first. SKILL: repo-watchlist.